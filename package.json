{
  "name": "dgg-clicker",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npx lerna bootstrap && npx lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.19.0"
  },
  "dependencies": {
    "@types/jest": "^24.0.24"
  }
}
